# Here the datatype converts integer to string
age = 25

age = str(age)
print(age, type(age))

# Here the datatype converts float into integer
height = 5.10

height = int(height)
print(height, type(height))

# Here i tried to covert string to integer , But it is unable to convert from string to integer
name = "VishnuVardhanBabu"

name = int(name)
print(name, type(name))